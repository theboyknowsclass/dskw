/**
 * Enum representing the corners of a rectangle.
 * Points are ordered as: top-left, top-right, bottom-right, bottom-left.
 */
export enum Corner {
  Top_Left = 0,
  Top_Right = 1,
  Bottom_Right = 2,
  Bottom_Left = 3,
}
